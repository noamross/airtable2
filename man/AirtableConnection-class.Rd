% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbi-connection.R
\docType{class}
\name{AirtableConnection-class}
\alias{AirtableConnection-class}
\alias{dbDisconnect,AirtableConnection-method}
\alias{dbIsValid,AirtableConnection-method}
\alias{dbListTables,AirtableConnection-method}
\alias{dbExistsTable,AirtableConnection,character-method}
\alias{dbListFields,AirtableConnection,character-method}
\alias{dbReadTable,AirtableConnection,character-method}
\alias{dbWriteTable,AirtableConnection,character-method}
\alias{dbRemoveTable,AirtableConnection,character-method}
\alias{dbGetInfo,AirtableConnection-method}
\title{Airtable DBI connection}
\usage{
\S4method{dbDisconnect}{AirtableConnection}(conn, ...)

\S4method{dbIsValid}{AirtableConnection}(dbObj, ...)

\S4method{dbListTables}{AirtableConnection}(conn, ...)

\S4method{dbExistsTable}{AirtableConnection,character}(conn, name, ...)

\S4method{dbListFields}{AirtableConnection,character}(conn, name, ...)

\S4method{dbReadTable}{AirtableConnection,character}(conn, name, ...)

\S4method{dbWriteTable}{AirtableConnection,character}(conn, name, value, overwrite = FALSE, append = FALSE, ...)

\S4method{dbRemoveTable}{AirtableConnection,character}(conn, name, ...)

\S4method{dbGetInfo}{AirtableConnection}(dbObj, ...)
}
\arguments{
\item{conn, dbObj}{An \code{AirtableConnection} object.}

\item{...}{Additional arguments passed to Airtable helpers.}

\item{name}{Table name.}

\item{value}{Data frame to write.}

\item{overwrite, append}{DBI write mode flags.}
}
\description{
Stores Airtable credentials and connection state for DBI methods. Use
\code{\link[=airtable2]{airtable2()}} with \code{\link[DBI:dbConnect]{DBI::dbConnect()}} to create connections.
}
\section{Capabilities and limitations}{

\describe{
\item{Reading tables}{\code{\link[DBI:dbReadTable]{dbReadTable()}} works on any accessible table. You
can also pass \code{"TableName WHERE <formula>"} as the name to filter records
using Airtable's formula syntax.}
\item{Writing tables}{\code{\link[DBI:dbWriteTable]{dbWriteTable()}} creates the table if it does not
exist (inferring field types from the data frame). When the table already
exists, \code{append = TRUE} creates new records; \code{overwrite = TRUE} syncs
(upsert + delete-missing) using the first column as the key.}
\item{No table removal}{Airtable's API cannot delete tables. Use the
Airtable web UI instead. \code{\link[DBI:dbRemoveTable]{dbRemoveTable()}} will error with a clear
message.}
\item{No SQL queries}{Arbitrary SQL is not supported. Use the high-level
helpers (\code{\link[=air_read]{air_read()}}, \code{\link[=air_write]{air_write()}}, \code{\link[=air_upsert]{air_upsert()}}, \code{\link[=air_sync]{air_sync()}}) for
more ergonomic access.}
\item{No transactions}{Airtable does not support database transactions.}
\item{Single-base or all-bases}{When a \code{base_id} is given, the connection
shows that base's tables directly. Without a \code{base_id}, all accessible
bases appear as schemas in the connection pane.}
}

For most use cases, the high-level functions like \code{\link[=air_read]{air_read()}}, \code{\link[=air_write]{air_write()}},
\code{\link[=air_upsert]{air_upsert()}}, and \code{\link[=air_sync]{air_sync()}} provide more ergonomic interfaces for
Airtable operations.
}

