% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-counter.R
\name{air_api_usage}
\alias{air_api_usage}
\title{Report Airtable API usage for a workspace}
\usage{
air_api_usage(workspace = NULL)
}
\arguments{
\item{workspace}{Workspace ID (starts with \code{wsp}). Defaults to the configured
default workspace (\code{getOption("airtable2.workspace_id")} or
\code{Sys.getenv("AIRTABLE_WORKSPACE_ID")}).}
}
\value{
A list of class \code{air_api_usage} with elements \code{workspace_id},
\code{count}, \code{since}, and \code{last}. Has a \code{{cli}} print method.
}
\description{
Returns this session's best-effort tally of API calls made against a
workspace during the current calendar month (UTC). Airtable free/team plans
cap each workspace at roughly 1000 calls per month and provide no way to
query the remaining quota, so \code{airtable2} keeps its own counter on disk.
}
\examples{
\dontrun{
air_api_usage("wspXXXXXXXXXXXXXX")
}
}
