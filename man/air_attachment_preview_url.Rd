% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_attachments.R
\name{air_attachment_preview_url}
\alias{air_attachment_preview_url}
\title{Get a stable preview URL for an Airtable attachment}
\usage{
air_attachment_preview_url(attachment)
}
\arguments{
\item{attachment}{A single attachment metadata list (as returned by
\code{air_read(..., attachments = "meta")}) \strong{or} a list of such attachment
lists. Each element should contain fields such as \code{id}, \code{url},
\code{filename}, \code{size}, and \code{type}.}
}
\value{
A character vector of \code{NA_character_}, one element per attachment.
The function always returns \code{NA} because a stable viewer URL cannot be
constructed from API metadata alone. See the @section above for the
correct manual workflow.
}
\description{
The Airtable REST API returns a \strong{temporary} download URL in the \code{url} field
of every attachment object. This URL is served from
\code{airtableusercontent.com} and \strong{expires after approximately 2 hours}.
Do not save or share the raw API \code{url} as a permanent link — it will stop
working shortly after it is issued.
}
\section{Stable attachment viewer URLs}{

Airtable maintains a separate \strong{attachment viewer URL} (served from
\code{airtable.com}) that does \strong{not} expire (unless the attachment or record is
deleted). This stable URL requires the recipient to have access to the base
or interface in which the attachment lives.

\strong{Crucially, the stable viewer URL cannot be derived from any field that the
REST API returns} (\code{id}, \code{url}, \code{filename}, \code{size}, \code{type}, \code{thumbnails}).
It is only obtainable by navigating to the record in the Airtable web app,
clicking the attachment to open its preview, and copying the URL from the
browser address bar.

Because no stable URL can be constructed programmatically from attachment
metadata, \code{air_attachment_preview_url()} always returns \code{NA_character_} and
emits an informative message directing users to the correct workflow.
}

\examples{
# Suppose you have attachment metadata from air_read():
att <- list(
  id       = "attABCDEF123456",
  url      = "https://v5.airtableusercontent.com/v3/some_signed_path",
  filename = "report.pdf",
  size     = 204800L,
  type     = "application/pdf"
)

# The temporary download URL (expires in ~2 hours):
att$url

# Attempting to get a stable preview URL — always NA with an explanation:
\dontrun{
air_attachment_preview_url(att)
}

# To obtain a permanent, shareable link for a colleague:
# 1. Open the record in the Airtable web app.
# 2. Click the attachment to open its viewer.
# 3. Copy the URL from the browser address bar (it is from airtable.com).
# 4. Share that URL — it does not expire as long as the attachment exists
#    and the recipient has access to the base.
}
