% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_demo.R
\name{air_demo}
\alias{air_demo}
\title{Run an interactive airtable2 demo walkthrough}
\usage{
air_demo(
  base_id = NULL,
  workspace_id = Sys.getenv("AIRTABLE_WORKSPACE_ID"),
  .token = NULL
)
}
\arguments{
\item{base_id}{Base ID to use. If \code{NULL}, checks the session default
(\code{\link[=air_set_base]{air_set_base()}}) and \code{AIRTABLE_BASE_ID} env var; calls
\code{\link[=air_demo_setup]{air_demo_setup()}} if nothing is found.}

\item{workspace_id}{Passed to \code{\link[=air_demo_setup]{air_demo_setup()}} if base creation is needed.}

\item{.token}{API token (see \code{\link[=air_set_token]{air_set_token()}}).}
}
\value{
Invisibly returns a list of results from each step.
}
\description{
Runs through the canonical airtable2 operations against a BollardsForArt
demo base, printing results at each step. If no \code{base_id} is provided and
none is set as the session default, calls \code{\link[=air_demo_setup]{air_demo_setup()}} first.
}
\details{
The BollardsForArt walkthrough covers:
\enumerate{
\item Read all records from the Artists table
\item Write a new artist record
\item Upsert (update or insert by Name)
\item Sync (diff-based create/update/delete)
\item Left-join local data with the Airtable table
\item View the base schema (including field descriptions)
\item View API usage
}

All operations are idempotent: upsert and sync operate by key (\code{Name}) so
running the demo multiple times will not accumulate duplicate records.
}
\examples{
\dontrun{
# Run with an existing demo base (set via air_set_base() or env var)
air_set_base("appXXXXXXXXXXXXXX")
air_demo()

# Or create a new demo base first (needs AIRTABLE_WORKSPACE_ID)
air_demo(workspace_id = Sys.getenv("AIRTABLE_WORKSPACE_ID"))
}
}
