% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_join.R
\name{air_left_join}
\alias{air_left_join}
\alias{air_inner_join}
\alias{air_full_join}
\title{Join local data with an Airtable table}
\usage{
air_left_join(x, base_id = NULL, table, by = NULL, ..., .token = NULL)

air_inner_join(x, base_id = NULL, table, by = NULL, ..., .token = NULL)

air_full_join(x, base_id = NULL, table, by = NULL, ..., .token = NULL)
}
\arguments{
\item{x}{A local data frame.}

\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table}{Table name or ID.}

\item{by}{Character vector of column name(s) to join on. If \code{NULL},
uses all column names shared between \code{x} and the remote table
(excluding \code{airtable_id} and \code{airtable_created_time}).}

\item{...}{Additional arguments passed to \code{\link[=air_read]{air_read()}}.}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A tibble.
}
\description{
Fetches a remote Airtable table and joins it with a local data frame.
These are convenience wrappers around \code{\link[=air_read]{air_read()}} plus base-R \code{merge()}.
The \code{...} are forwarded to \code{\link[=air_read]{air_read()}} (e.g. \code{formula}, \code{fields}).
}
\examples{
\dontrun{
scores <- tibble::tibble(Name = c("Alice", "Bob"), Score = c(90, 85))
air_left_join(scores, "appXXXX", "Contacts", by = "Name")
}
}
