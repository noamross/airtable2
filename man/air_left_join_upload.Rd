% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_join.R
\name{air_left_join_upload}
\alias{air_left_join_upload}
\title{Upload local data to matched Airtable records}
\usage{
air_left_join_upload(
  x,
  table,
  base_id = NULL,
  by = NULL,
  fields = NULL,
  add_fields = "yes",
  typecast = TRUE,
  complex_fields = c("error", "warn", "json"),
  progress = NULL,
  .token = NULL
)
}
\arguments{
\item{x}{A local data frame whose columns should be uploaded.}

\item{table}{Table name or ID.}

\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}). If \code{NULL}, uses the session
default set by \code{\link[=air_set_base]{air_set_base()}} or the \code{AIRTABLE_BASE_ID} environment
variable.}

\item{by}{The join key: a column present in both \code{x} and the remote table
(e.g. \code{"Name"}), or a named character vector \code{c(local = "remote")} when
the local and remote key columns differ. If \code{NULL}, uses common column
names (dplyr-style) and messages which key was chosen.}

\item{fields}{Optional character vector limiting which of \code{x}'s columns to
upload. Defaults to all columns of \code{x} except the key and the Airtable
meta columns (\code{airtable_id}, \code{airtable_created_time}).}

\item{add_fields}{What to do when an upload column does not exist in the
table: \code{"yes"} (default) creates it, \code{"warn"} warns and drops it,
\code{"error"} errors. Passed through to \code{\link[=air_upsert]{air_upsert()}}.}

\item{typecast}{If \code{TRUE} (default), Airtable will attempt to coerce values
to match field types. Passed through to \code{\link[=air_upsert]{air_upsert()}}.}

\item{complex_fields}{What to do with complex (nested list or data-frame)
columns: \code{"error"} (default) aborts, \code{"warn"} drops them, \code{"json"}
serializes them as JSON text. Passed through to \code{\link[=air_upsert]{air_upsert()}}.}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a progress bar.
Passed through to \code{\link[=air_upsert]{air_upsert()}}.}

\item{.token}{Optional API token.}
}
\value{
Invisibly, a tibble of the records that were updated: their
\code{airtable_id}, the key, and the changed field values.
}
\description{
\code{air_left_join_upload()} is the complement of \code{\link[=air_left_join]{air_left_join()}}. Instead of
pulling remote data \emph{into} a local data frame, it pushes the columns that a
local data frame \code{x} carries \emph{onto} matching records that already exist in an
Airtable table, matching by a join key. It enriches existing records with
new or changed field values.
}
\details{
Matching is by \code{by} (a key present in both \code{x} and the remote table). For
each matched record, only the upload columns whose value is \strong{new} (the
field does not yet exist remotely) or \strong{changed} (differs from the current
remote value) are sent, minimising API calls. Columns whose value already
equals the remote value are not re-sent.

This function never inserts new records (unmatched local rows are skipped and
counted) and never deletes remote fields. To insert as well as update, use
\code{\link[=air_upsert]{air_upsert()}}.

The remote table is read minimally: only the key field plus any to-upload
fields that already exist are fetched. The actual write is delegated to
\code{\link[=air_upsert]{air_upsert()}} (matching by \code{airtable_id}), so batching, throttling, API
counting, and field creation are reused.
}
\examples{
\dontrun{
# Local scores you computed; push them onto existing Contacts by Name.
scores <- tibble::tibble(Name = c("Alice", "Bob"), Score = c(90, 85))
air_left_join_upload(scores, "Contacts", "appXXXX", by = "Name")

# Different local/remote key names.
air_left_join_upload(scores, "Contacts", "appXXXX", by = c(person = "Name"))
}
}
\seealso{
\code{\link[=air_left_join]{air_left_join()}} for the read direction, \code{\link[=air_upsert]{air_upsert()}} to also
insert new records.
}
