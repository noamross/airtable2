% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_meta.R
\name{air_meta_init}
\alias{air_meta_init}
\title{Seed the _metadata table from the live schema}
\usage{
air_meta_init(base_id, meta_table = "_metadata", .token = NULL)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}). If \code{NULL}, uses the session
default set by \code{\link[=air_set_base]{air_set_base()}} or the \code{AIRTABLE_BASE_ID} environment
variable.}

\item{meta_table}{Name of the table to store metadata in.
Default \code{"_metadata"}.}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
Invisible upsert result.
}
\description{
Reads the current base schema via \code{\link[=air_meta]{air_meta()}} and upserts it into a
designated table within the same base.  Run this \strong{once} to initialise the
metadata store, then edit the table in Airtable and call \code{\link[=air_meta_sync]{air_meta_sync()}}
to push changes back.
}
\examples{
\dontrun{
# Initialise the _metadata table
air_meta_init("appXXXXXX")

# Use a custom table name
air_meta_init("appXXXXXX", meta_table = "_docs")
}
}
