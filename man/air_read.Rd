% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_read.R
\name{air_read}
\alias{air_read}
\title{Read records from an Airtable table}
\usage{
air_read(
  table,
  base_id = NULL,
  view = NULL,
  fields = NULL,
  formula = NULL,
  sort = NULL,
  max_records = Inf,
  page_size = 100L,
  coerce = TRUE,
  attachments = c("meta", "file", "blob"),
  attachment_dir = NULL,
  parallel = NULL,
  progress = NULL,
  .token = NULL
)
}
\arguments{
\item{table}{Table name or ID.}

\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}). If \code{NULL}, uses the session
default set by \code{\link[=air_set_base]{air_set_base()}} or the \code{AIRTABLE_BASE_ID} environment
variable.}

\item{view}{Optional view name or ID to filter by.}

\item{fields}{Optional character vector of field names to return.}

\item{formula}{Optional Airtable formula string for filtering.}

\item{sort}{Optional named character vector for sorting (names = field names,
values = \code{"asc"} or \code{"desc"}).}

\item{max_records}{Maximum number of records to return. Default \code{Inf} (all).}

\item{page_size}{Records per page (max 100).}

\item{coerce}{If \code{TRUE} (default), fetches schema and coerces types. If
\code{FALSE}, returns raw values (faster but untyped).}

\item{attachments}{How to handle attachment fields: \code{"meta"} (default) keeps
only metadata (filename, url, size, type); \code{"file"} downloads to
\code{attachment_dir}; \code{"blob"} downloads as in-memory raw vectors.}

\item{attachment_dir}{Directory for downloading attachments (required when
\code{attachments = "file"}). Files are saved as
\code{{attachment_dir}/{record_id}/{filename}}.}

\item{parallel}{Logical or \code{NULL}. If \code{TRUE}, attachment downloads use
\code{\link[httr2:req_perform_parallel]{httr2::req_perform_parallel()}} (up to 5 concurrent). If \code{NULL}, uses
option \code{airtable2.parallel} or env var \code{AIRTABLE2_PARALLEL} (default \code{TRUE}).}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a cli progress bar for
paginated requests. If \code{NULL} (default), uses option \code{airtable2.progress.bar}
or env var \code{AIRTABLE2_PROGRESS_BAR}.}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A tibble with columns \code{airtable_id}, \code{airtable_created_time}, and
one column per field.
}
\description{
High-level function that auto-paginates, fetches schema for type coercion,
and returns a properly typed tibble.
}
\examples{
\dontrun{
# Read all records from a table
df <- air_read("Contacts", "appXXXXXX")

# Read specific fields with a filter
df <- air_read("Contacts", "appXXXXXX",
  fields = c("Name", "Email"),
  formula = "{Age} > 30"
)

# Download attachments to disk
df <- air_read("Projects", "appXXXXXX",
  attachments = "file",
  attachment_dir = "downloads/"
)
}
}
