% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_read.R
\name{air_read}
\alias{air_read}
\title{Read records from an Airtable table}
\usage{
air_read(
  base_id,
  table,
  view = NULL,
  fields = NULL,
  formula = NULL,
  sort = NULL,
  max_records = Inf,
  page_size = 100L,
  coerce = TRUE,
  .token = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table}{Table name or ID.}

\item{view}{Optional view name or ID to filter by.}

\item{fields}{Optional character vector of field names to return.}

\item{formula}{Optional Airtable formula string for filtering.}

\item{sort}{Optional named character vector for sorting (names = field names,
values = \code{"asc"} or \code{"desc"}).}

\item{max_records}{Maximum number of records to return. Default \code{Inf} (all).}

\item{page_size}{Records per page (max 100).}

\item{coerce}{If \code{TRUE} (default), fetches schema and coerces types. If
\code{FALSE}, returns raw values (faster but untyped).}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A tibble with columns \code{airtable_id}, \code{airtable_created_time}, and
one column per field.
}
\description{
High-level function that auto-paginates, fetches schema for type coercion,
and returns a properly typed tibble.
}
\examples{
\dontrun{
# Read all records from a table
df <- air_read("appXXXXXX", "Contacts")

# Read specific fields with a filter
df <- air_read("appXXXXXX", "Contacts",
  fields = c("Name", "Email"),
  formula = "{Age} > 30"
)
}
}
