% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_attachments.R
\name{air_read_attachments}
\alias{air_read_attachments}
\title{Read attachments from records}
\usage{
air_read_attachments(
  base_id,
  table,
  field,
  record_ids = NULL,
  dest = c("blob", "file"),
  dir = NULL,
  .token = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table}{Table name or ID.}

\item{field}{Name of the attachment field.}

\item{record_ids}{Optional character vector of specific record IDs to fetch.
If \code{NULL}, fetches all records.}

\item{dest}{Either \code{"blob"} (return raw content as list-column) or \code{"file"}
(save to disk).}

\item{dir}{Directory to save files to (required if \code{dest = "file"}).}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A tibble with \code{airtable_id}, \code{filename}, \code{url}, and either \code{blob}
(raw list-column) or \code{local_path} (character).
}
\description{
Downloads attachment files from a specified field, returning them as either
in-memory blobs or saved files.
}
