% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_dump.R
\name{air_restore}
\alias{air_restore}
\title{Restore a base from a dump}
\usage{
air_restore(
  dump,
  base_name = NULL,
  workspace_id = NULL,
  attachments = c("file", "meta"),
  attachment_dir = NULL,
  .token = NULL
)
}
\arguments{
\item{dump}{Either a list (from \code{air_dump(format = "list")}) or a path to
a dump directory (from \code{air_dump(format = "json")} or \code{air_dump(format = "csv")}).}

\item{base_name}{Name for the new base. If \code{NULL}, uses a generated name.}

\item{workspace_id}{Workspace ID to create the base in.}

\item{attachments}{How to handle attachment fields: \code{"meta"} (default) keeps
only metadata (filename, url, size, type); \code{"file"} downloads to
\code{attachment_dir}; \code{"blob"} downloads as in-memory raw vectors.}

\item{attachment_dir}{Directory for downloading attachments (required when
\code{attachments = "file"}). Files are saved as
\code{{attachment_dir}/{record_id}/{filename}}.}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
The new base ID (invisibly).
}
\description{
Recreates a base from output of \code{\link[=air_dump]{air_dump()}}. When \code{attachments} is
\code{"file"}, uploads attachment files from the dump directory after record
creation. For CSV dumps, automatically detects and parses the flattened format.
}
\section{Linked-record fields}{

Linked-record fields (\code{multipleRecordLinks}) and the computed fields that
depend on them (\code{rollup}, \code{lookup}, \code{count}) are skipped during the initial
field-creation pass because their options reference base-specific IDs that
do not exist in the new base. After all tables and records have been
created, \code{restore_linked_fields()} recreates these fields, remapping the
linked-table and record-link-field references to the new base's IDs.

Note that the link FIELDS are recreated but their CELL VALUES (the specific
record-to-record connections) are NOT repopulated: the old record IDs stored
in the dump do not correspond to the newly created records' IDs. Re-linking
cell values is left for a future pass.
}

\examples{
\dontrun{
# Restore from a directory dump
air_restore("backup/", workspace_id = "wspXXXXXX")

# Restore from a CSV dump
air_restore("backup/", workspace_id = "wspXXXXXX", format = "csv")
}
}
