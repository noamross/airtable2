% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_sync.R
\name{air_sync}
\alias{air_sync}
\title{Smart sync: diff-based upsert + delete}
\usage{
air_sync(
  data,
  table,
  key,
  base_id = NULL,
  hash_fields = NULL,
  delete_missing = TRUE,
  typecast = TRUE,
  add_fields = c("error", "warn", "yes"),
  attachments = c("meta", "file", "blob"),
  attachment_dir = NULL,
  progress = NULL,
  .token = NULL
)
}
\arguments{
\item{data}{A data frame representing the desired state of the table.
May contain computed field columns (they are ignored).}

\item{table}{Table name or ID.}

\item{key}{Column name in \code{data} that uniquely identifies records (used as
the merge field for upsert). Must be a single field.}

\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}). If \code{NULL}, uses the session
default set by \code{\link[=air_set_base]{air_set_base()}} or the \code{AIRTABLE_BASE_ID} environment
variable.}

\item{hash_fields}{Character vector of fields to include in the change-
detection hash. If \code{NULL} (default), all non-key, non-computed,
non-attachment fields are used. Computed and attachment fields are always
excluded even if explicitly listed.}

\item{delete_missing}{If \code{TRUE} (default), records in Airtable that are not
in \code{data} will be deleted.}

\item{typecast}{If \code{TRUE} (default), Airtable will attempt to coerce values.}

\item{add_fields}{What to do when \code{data} contains columns not in the table.
Passed to \code{\link[=air_upsert]{air_upsert()}}. Default is \code{"error"}.}

\item{attachments}{How to handle attachment fields: \code{"meta"} (default) keeps
only metadata (filename, url, size, type); \code{"file"} downloads to
\code{attachment_dir}; \code{"blob"} downloads as in-memory raw vectors.}

\item{attachment_dir}{Directory for downloading attachments (required when
\code{attachments = "file"}). Files are saved as
\code{{attachment_dir}/{record_id}/{filename}}.}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a cli progress bar for
read and upsert operations. If \code{NULL} (default), uses option
\code{airtable2.progress.bar} or env var \code{AIRTABLE2_PROGRESS_BAR}.}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A list with counts: \code{created}, \code{updated}, \code{deleted}, \code{unchanged}
(invisibly).
}
\description{
Compares local data against the current table contents using a hash of
specified fields, then creates/updates/deletes as needed.
}
\details{
Computed fields (formulas, rollups, autoNumber, createdTime,
lastModifiedTime, createdBy, lastModifiedBy, etc.) are automatically:
\itemize{
\item Excluded from the change-detection hash (they change server-side and
would cause spurious "changed" detections on every sync)
\item Excluded from the upload payload (the API rejects writes to them)
}

Attachment fields (\code{multipleAttachments}) are always excluded from the
change-detection hash because their URLs are volatile (expire hourly).
When \code{attachments} is \code{"file"} or \code{"blob"}, attachment content is uploaded
for newly created records after the sync completes.
}
\examples{
\dontrun{
desired <- data.frame(Name = c("Alice", "Bob"), Age = c(30, 26))
result <- air_sync(desired, "Contacts", key = "Name", base_id = "appXXXXXX")
result$created
result$unchanged
}
}
