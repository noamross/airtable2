% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/air_upsert.R
\name{air_upsert}
\alias{air_upsert}
\title{Upsert records into an Airtable table}
\usage{
air_upsert(
  base_id,
  table,
  data,
  merge_on,
  typecast = TRUE,
  add_fields = c("error", "warn", "yes"),
  .token = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table}{Table name or ID.}

\item{data}{A data frame of records to upsert. May include an \code{airtable_id}
column for direct record matching. Computed field columns are silently
dropped.}

\item{merge_on}{Character vector of 1-3 field names to match on (for records
without an \code{airtable_id}).}

\item{typecast}{If \code{TRUE} (default), Airtable will attempt to coerce values.}

\item{add_fields}{What to do when \code{data} contains columns not in the table:
\itemize{
\item \code{"error"} (default): error if unknown columns exist.
\item \code{"warn"}: warn and drop unknown columns.
\item \code{"yes"}: create missing fields before upserting (as \code{singleLineText}).
}}

\item{.token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A list with \code{created} and \code{updated} character vectors of record IDs
(invisibly).
}
\description{
Uses Airtable's native upsert (PATCH with \code{performUpsert}) to create or
update records based on merge fields. Supports two matching modes:
}
\details{
\itemize{
\item If \code{data} contains an \code{airtable_id} column, records with non-NA IDs are
updated directly by record ID (more efficient).
\item Records without an \code{airtable_id} (or where it is \code{NA}) are matched using
the \code{merge_on} field(s) via Airtable's upsert mechanism.
}

Computed fields (formulas, rollups, autoNumber, createdTime,
lastModifiedTime, etc.) are automatically excluded from the upload payload.
Optionally creates missing columns.
}
\examples{
\dontrun{
data <- data.frame(Name = c("Alice", "Bob"), Age = c(31, 26))
result <- air_upsert("appXXXXXX", "Contacts", data, merge_on = "Name")
result$created
result$updated
}
}
