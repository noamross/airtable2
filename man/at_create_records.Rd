% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/at-records.R
\name{at_create_records}
\alias{at_create_records}
\title{Create records in a table}
\usage{
at_create_records(
  base_id,
  table_id,
  records,
  typecast = FALSE,
  token = NULL,
  progress = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table_id}{Table name or ID.}

\item{records}{A list of record objects. Each should be a list with a
\code{fields} element (a named list of field values).}

\item{typecast}{If \code{TRUE}, Airtable will attempt to cast values to the
correct type.}

\item{token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a cli progress bar for
batch operations. If \code{NULL} (default), uses option \code{airtable2.progress.bar}
or env var \code{AIRTABLE2_PROGRESS_BAR} (both default to \code{FALSE}).}
}
\value{
A list of created record objects (with assigned IDs).
}
\description{
Creates up to 10 records per API call. If more than 10 records are provided,
they are automatically batched.
}
\examples{
\dontrun{
records <- list(
  list(fields = list(Name = "Alice", Age = 30)),
  list(fields = list(Name = "Bob",   Age = 25))
)
created <- at_create_records("appXXXXXXXXXXXXXX", "Contacts", records)
vapply(created, function(r) r$id, character(1))
}
}
