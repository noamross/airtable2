% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/at-records.R
\name{at_list_records}
\alias{at_list_records}
\title{List records from a table}
\usage{
at_list_records(
  base_id,
  table_id,
  fields = NULL,
  formula = NULL,
  sort = NULL,
  view = NULL,
  max_records = Inf,
  page_size = 100L,
  cell_format = NULL,
  time_zone = NULL,
  user_locale = NULL,
  return_fields_by_id = FALSE,
  token = NULL,
  progress = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table_id}{Table name or ID.}

\item{fields}{Character vector of field names to return.}

\item{formula}{Airtable formula string for filtering.}

\item{sort}{A named character vector: names are field names, values are
\code{"asc"} or \code{"desc"}.}

\item{view}{View name or ID to filter by.}

\item{max_records}{Maximum total records to return.}

\item{page_size}{Records per page (max 100).}

\item{cell_format}{Either \code{"json"} (default) or \code{"string"}.}

\item{time_zone}{Time zone for date formatting (when \code{cell_format = "string"}).}

\item{user_locale}{Locale for date formatting.}

\item{return_fields_by_id}{If \code{TRUE}, use field IDs as keys instead of names.}

\item{token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a cli progress bar for
batch operations. If \code{NULL} (default), uses option \code{airtable2.progress.bar}
or env var \code{AIRTABLE2_PROGRESS_BAR} (both default to \code{TRUE}).}
}
\value{
A list of record objects (each with \code{id}, \code{createdTime}, \code{fields}).
}
\description{
List records from a table
}
\examples{
\dontrun{
# List all records from a table
records <- at_list_records("appXXXXXXXXXXXXXX", "Contacts")

# Filter records with a formula
records <- at_list_records(
  "appXXXXXXXXXXXXXX", "Contacts",
  formula = "{Active} = TRUE()",
  fields  = c("Name", "Email")
)
}
}
