% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/at-other.R
\name{at_sitrep}
\alias{at_sitrep}
\title{Summarize Airtable situation report}
\usage{
at_sitrep(token = NULL)
}
\arguments{
\item{token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}
}
\value{
A list with:
\itemize{
\item \code{user}: User info from \code{\link[=at_whoami]{at_whoami()}} (id, and \code{email}/\code{scopes} if the
token grants them)
\item \code{scopes}: Character vector of token scopes, or \code{NULL} if not exposed
\item \code{bases}: Tibble of accessible bases (\code{id}, \code{name}, \code{permissionLevel})
\item \code{error}: \code{NULL} if successful, otherwise a message string
}

Note: the non-enterprise Airtable API does not expose workspace names or
the workspace each base belongs to, so workspaces cannot be enumerated
from a token alone.
}
\description{
Safely probes the current token to determine what workspaces and bases
are accessible. Modelled after \code{\link[usethis:git_sitrep]{usethis::git_sitrep()}} and \code{\link[usethis:proj_sitrep]{usethis::proj_sitrep()}}.
Useful for debugging authentication issues and understanding the scope of
the current token.
}
\examples{
\dontrun{
# Check what your current token can access
info <- at_sitrep()
print(info$user)
print(info$scopes)
head(info$bases)
}
}
