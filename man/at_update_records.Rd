% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/at-records.R
\name{at_update_records}
\alias{at_update_records}
\title{Update multiple records}
\usage{
at_update_records(
  base_id,
  table_id,
  records,
  method = c("PATCH", "PUT"),
  typecast = FALSE,
  upsert_fields = NULL,
  token = NULL,
  progress = NULL
)
}
\arguments{
\item{base_id}{Base ID (e.g., \code{"appXXXXXX"}).}

\item{table_id}{Table name or ID.}

\item{records}{A list of record objects. Each should be a list with \code{id} and
\code{fields} elements. For upsert, \code{id} can be omitted.}

\item{method}{Either \code{"PATCH"} (partial update) or \code{"PUT"} (destructive).}

\item{typecast}{If \code{TRUE}, Airtable will attempt to cast values.}

\item{upsert_fields}{Character vector (1-3 fields) to merge on for upsert.
If \code{NULL}, performs a standard update.}

\item{token}{Personal access token (resolved via \code{\link[=air_token]{air_token()}} if \code{NULL}).}

\item{progress}{Logical or \code{NULL}. If \code{TRUE}, shows a cli progress bar for
batch operations. If \code{NULL} (default), uses option \code{airtable2.progress.bar}
or env var \code{AIRTABLE2_PROGRESS_BAR} (both default to \code{FALSE}).}
}
\value{
A list with \code{records}, and (for upserts) \code{createdRecords} and
\code{updatedRecords} character vectors.
}
\description{
Updates records using PATCH (partial) or PUT (destructive). Handles
auto-batching in groups of 10. Also supports upsert via the
\code{upsert_fields} argument.
}
